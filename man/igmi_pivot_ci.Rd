% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_inference.R
\name{igmi_pivot_ci}
\alias{igmi_pivot_ci}
\title{Exact studentized confidence region via the Frechet scatter}
\usage{
igmi_pivot_ci(studies, weights = NULL, level = 0.95)
}
\arguments{
\item{studies}{List of \code{igmi_gaussian} objects.}

\item{weights}{Positive weights (default equal).}

\item{level}{Confidence level.}
}
\value{
List: \code{theta_bar}, \code{Omega} (Frechet scatter),
\code{f_quantile} (\verb{F_\{m,K-m\}} at \code{level}), \code{level}, \code{df}; for \code{m = 1}
also \code{ci} and \code{se} (\code{sqrt(V_loc/(K-1))}).
}
\description{
Under proportional total covariances (\verb{V_i = c_i V_0}, weights
proportional to \code{1/c_i} -- automatic for \code{m = 1} with total-variance
weights), igmi_notes Prop 5.16 gives, exactly at every \code{K >= m + 1},
\verb{((K-m)/m) (theta_bar - theta)' Omega_hat^\{-1\} (theta_bar - theta) ~ F_\{m, K-m\}}. For \code{m = 1} this is
\verb{theta_bar +/- t_\{K-1\} sqrt(V_loc / (K-1))} -- the HKSJ interval in
IGMI notation (Rem 5.18(a)): no estimate of the between-study
covariance \code{T} enters. Outside proportionality the region is
approximate; \code{\link[=igmi_bootstrap]{igmi_bootstrap()}} is the Phase-3 default.
}
