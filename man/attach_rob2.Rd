% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_adapter.R
\name{attach_rob2}
\alias{attach_rob2}
\title{Attach RoB2 judgements to tidy estimates}
\usage{
attach_rob2(estimates, rob)
}
\arguments{
\item{estimates}{Tidy estimates from \code{\link[=read_rm5_estimates]{read_rm5_estimates()}} /
\code{\link[=read_zip_estimates]{read_zip_estimates()}} (or their row-bind).}

\item{rob}{List from \code{\link[=read_rob2]{read_rob2()}}.}
}
\value{
\code{estimates} with six \verb{rob2_*} columns and \code{rob2_level} appended.
}
\description{
Three-stage join, each stage only filling rows still unmatched:
(1) estimate-level on
\verb{(review, analysis_group, analysis_number, subgroup, study)} where
those keys exist (zip-derived rows); (2) the study-level table on
\verb{(review, study)}; (3) the estimate-level table collapsed to study
level (\code{\link[=collapse_rob2_study]{collapse_rob2_study()}}) on \verb{(review, study)}. Later stages
fill \emph{per domain}: a row whose estimate-level RoB2 row exists but
carries empty judgements (30\% of matched rows on the real corpus)
still gets its missing domains backfilled from the study level.
Review ids are canonicalized with \code{\link[=norm_review_id]{norm_review_id()}} on both sides.
The \code{rob2_level} column records the finest stage whose \emph{key} matched
(\code{"estimate"}, \code{"study"}, \code{"study_from_estimates"}, or \code{NA});
individual domain values may come from a coarser stage via backfill.
}
