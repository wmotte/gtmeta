% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_barycenter.R
\name{fr_dist}
\alias{fr_dist}
\title{Fisher-Rao distance on the full Gaussian manifold}
\usage{
fr_dist(N1, N2)
}
\arguments{
\item{N1, N2}{\code{igmi_gaussian} objects.}
}
\value{
Nonnegative scalar.
}
\description{
Exact variational formula (igmi_notes Thm 6.11):
\verb{d_FR(N1, N2) = (1/2) min over C in Skew(m) of ||log(s1^\{-1/2\} z2(C) s1^\{-1/2\})||_F}, where \code{z2(C)} runs over the
explicit fiber of the Riemannian submersion (normal form of Lem 6.9)
and \code{s1 = z1(0)}. For \code{m = 1} the fiber is a point and the formula is
closed (it must agree with \code{\link[=fr_dist_m1]{fr_dist_m1()}} -- tested); for \code{m = 2} it
is a one-variable minimization. The minimization over
\code{m(m-1)/2} fiber parameters is done numerically from \code{C = 0}
(the canonical section, also an upper bound).
}
