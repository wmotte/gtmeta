% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_heterogeneity.R
\name{igmi_mm_cov}
\alias{igmi_mm_cov}
\title{Matrix method-of-moments estimator of the between-study covariance}
\usage{
igmi_mm_cov(studies, weights = NULL)
}
\arguments{
\item{studies}{List of \code{igmi_gaussian} objects.}

\item{weights}{Positive weights (default equal).}
}
\value{
List: \code{T_hat} (PSD-projected), \code{T_raw} (unbiased, possibly
indefinite), \code{Omega} (Frechet scatter), \code{E0_Omega}
(\verb{sum_i w_i(1-w_i) Sigma_i}), \code{denom} (\verb{1 - sum w_i^2}).
}
\description{
The matrix analogue of the trace moment estimator of Prop 5.11(2)
(igmi_notes Rem 5.12, Phase-5.B update): with the Frechet scatter
\verb{Omega_hat = sum_i w_i (theta_i - theta_bar)(theta_i - theta_bar)'},
exactly \verb{E[Omega_hat] = sum_i w_i(1-w_i) Sigma_i + (1 - sum w_i^2) T},
so the raw estimator
\verb{T_raw = (Omega_hat - sum_i w_i(1-w_i) Sigma_i) / (1 - sum w_i^2)}
is unbiased for \code{T} (fixed weights, known \code{Sigma_i}); \code{T_hat} is its
Frobenius projection onto the PSD cone (eigenvalue truncation at
zero). \code{tr(T_raw)} equals \code{tr_T_hat_raw} of \code{\link[=igmi_heterogeneity]{igmi_heterogeneity()}}
exactly, hence for \code{m = 1} with precision weights the untruncated
DerSimonian-Laird \code{tau^2}; the matrix form is a fixed-weights variant
of the multivariate DL family of Jackson, White & Thompson (2010).
}
