% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_barycenter.R
\name{fr_mean}
\alias{fr_mean}
\title{Fisher-Rao Frechet mean}
\usage{
fr_mean(studies, weights = NULL, tol = 1e-10, max_iter = 200L)
}
\arguments{
\item{studies}{List of \code{igmi_gaussian} objects.}

\item{weights}{Positive weights (default equal).}

\item{tol}{Convergence tolerance (gradient norm / step criterion).}

\item{max_iter}{Maximum iterations (centered fixed point).}
}
\value{
Object of class \code{igmi_gaussian} (the mean), with attributes
\code{frechet_value} (the minimized functional) and \code{regime}.
}
\description{
Three regimes, matching the settled mathematics:
\itemize{
\item \code{m = 1}: the manifold is hyperbolic (Hadamard), so the weighted
Frechet mean exists and is unique (igmi_notes Cor 6.2(1)); computed
by direct 2-parameter minimization of the Frechet functional with
the closed-form distance \code{\link[=fr_dist_m1]{fr_dist_m1()}}.
\item centered family (all \code{theta_i} equal): Cartan-Hadamard again
(Cor 6.2(2)); computed by the classical Karcher fixed-point
iteration for the affine-invariant mean,
\verb{X <- X^\{1/2\} exp(sum_i w_i log(X^\{-1/2\} Sigma_i X^\{-1/2\})) X^\{1/2\}}
(the 1/sqrt(2) metric scaling does not change the minimizer).
\item general \code{m >= 2}: unique within the Karcher ball of Cor 6.21;
\code{\link[=fr_karcher_check]{fr_karcher_check()}} is run first (warning if not verifiably
admissible), then the Frechet functional is minimized numerically
over \verb{(theta, chol Sigma)} with the variational distance
\code{\link[=fr_dist]{fr_dist()}} (nested optimization -- small \code{m}, small \code{K} only).
}
}
