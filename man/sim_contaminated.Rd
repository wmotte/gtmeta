% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_contaminated}
\alias{sim_contaminated}
\title{Simulate a contaminated meta-analysis}
\usage{
sim_contaminated(
  k,
  mu = 0,
  tau2 = 0,
  sei = NULL,
  sei_range = c(0.1, 0.5),
  eps = 0.1,
  shift = 1,
  understate = 1
)
}
\arguments{
\item{k}{Number of studies.}

\item{mu}{True common/mean effect.}

\item{tau2}{Between-study variance.}

\item{sei}{Optional vector of within-study standard errors (length k);
if \code{NULL}, drawn uniformly from \code{sei_range}.}

\item{sei_range}{Range to draw \code{sei} from when not supplied.}

\item{eps}{Contamination fraction in \verb{[0, 1)}.}

\item{shift}{Mean shift added to contaminated studies' true effects.}

\item{understate}{Factor by which contaminated studies understate
their standard error (\code{1} = none).}
}
\value{
As \code{\link[=sim_meta]{sim_meta()}}, plus logical column \code{is_outlier}; the
reported \code{sei} column carries the understated values.
}
\description{
Two contamination mechanisms on top of \code{\link[=sim_meta]{sim_meta()}}, applied to a
random fraction \code{eps} of studies:
\itemize{
\item a mean shift of \code{shift} (absolute, on the effect scale), and/or
\item an understated standard error: the \emph{reported} \code{sei} is the true
sampling standard deviation divided by \code{understate} (so
\code{understate = 2} means the study is twice as noisy as it claims).
This is the breakdown/robustness regime of TODO.md Phase 4.B.
}
}
