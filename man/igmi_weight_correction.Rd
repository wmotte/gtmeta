% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_inference.R
\name{igmi_weight_correction}
\alias{igmi_weight_correction}
\title{Coverage correction factors for estimated precision weights (m = 1)}
\usage{
igmi_weight_correction(weights, nu)
}
\arguments{
\item{weights}{True (or plug-in) normalised precision weights.}

\item{nu}{Within-study degrees of freedom (recycled to length of
\code{weights}).}
}
\value{
List: \code{variance_inflation}, \code{naive_bias_factor},
\code{deficit_factor} (multiply the naive squared standard error by this
to correct the interval at leading order).
}
\description{
igmi_notes Prop 5.17(3): with \verb{hat s_i^2 ~ s_i^2 chi^2_\{nu_i\}/nu_i}
and plug-in precision weights, the true variance of the pooled
estimate is inflated by \verb{1 + 2 sum_i w_i(1-w_i)/nu_i + O(nu^-2)},
the naive plug-in variance \verb{1/hat S} is biased down by the same
leading amount, and the naive interval's variance deficit is the
factor \verb{1 + 4 sum_i w_i(1-w_i)/nu_i + O(nu^-2)}. The point estimate
stays exactly unbiased (Prop 5.17(1)). Requires \code{min(nu) >= 9} for
the remainder control.
}
