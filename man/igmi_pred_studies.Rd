% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_prediction.R
\name{igmi_pred_studies}
\alias{igmi_pred_studies}
\title{Prediction-model-performance study objects from C-statistic and O:E}
\usage{
igmi_pred_studies(
  c_stat,
  c_se,
  oe,
  oe_se,
  study = NULL,
  rho = 0,
  transform = TRUE
)
}
\arguments{
\item{c_stat}{Concordance statistics (in \verb{(0,1)} when \code{transform}).}

\item{c_se}{Standard errors of \code{c_stat} (on the same scale).}

\item{oe}{Observed-to-expected ratios (\verb{> 0} when \code{transform}).}

\item{oe_se}{Standard errors of \code{oe} (on the same scale).}

\item{study}{Optional study labels (defaults to \code{seq_along(c_stat)}).}

\item{rho}{Plug-in within-study correlation between \verb{logit C} and
\verb{log O:E} (default 0); must satisfy \verb{-1 < rho < 1}.}

\item{transform}{If \code{TRUE} (default), delta-method transform natural-
scale inputs to \verb{(logit C, log O:E)}; if \code{FALSE}, inputs are already
on those scales.}
}
\value{
List of study objects \code{list(study =, theta =, Sigma =)}, the
same contract as \code{\link[=igmi_study_list]{igmi_study_list()}}.
}
\description{
Turns per-study discrimination and calibration summaries into IGMI study
objects on the \verb{(logit C, log O:E)} scale (igmi_notes Definition 8.4).
Inputs are given on their natural scales and transformed by the delta
method: \verb{se(logit C) = se(C) / (C(1-C))} and
\verb{se(log O:E) = se(O:E) / (O:E)}. Pass already-transformed inputs with
\code{transform = FALSE} (then \code{c_stat}/\code{oe} are taken as \verb{logit C}/\verb{log O:E}
and \code{c_se}/\code{oe_se} as their SEs on those scales).
}
\details{
The within-study correlation \code{rho} between discrimination and
calibration is a plug-in (unidentified from marginal reports); the
trace-precision-weighted BW point estimate is invariant to it
(Corollary 8.5), which the accompanying tests assert exactly.
}
\seealso{
\code{\link[=igmi_study_list]{igmi_study_list()}}, \code{\link[=igmi_dta_studies]{igmi_dta_studies()}}
}
