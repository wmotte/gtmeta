% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_network}
\alias{sim_network}
\title{Simulate pairwise contrasts on an evidence graph with loop inconsistency}
\usage{
sim_network(edges, d, omega = 0, n_per_edge = 3, sei_range = c(0.1, 0.5))
}
\arguments{
\item{edges}{Two-column character matrix (or data frame) of treatment
pairs \verb{(t1, t2)}, orientation t1 -> t2.}

\item{d}{Named numeric vector of true absolute effects; names must
cover all treatments in \code{edges}.}

\item{omega}{Numeric vector of per-edge inconsistency offsets
(recycled; default 0).}

\item{n_per_edge}{Number of studies per edge (recycled).}

\item{sei_range}{Range for study standard errors.}
}
\value{
Data frame \verb{(study, t1, t2, edge, yi, sei)} with attributes
\code{d}, \code{omega}.
}
\description{
Truth: vertex potentials \code{d} (absolute effects, gauge-fixed at the
reference) generate consistent edge contrasts
\code{d[t2] - d[t1]}; an inconsistency vector \code{omega} (one entry per edge,
default all zero) is added on top. With \code{omega = 0} every cycle sum of
true contrasts vanishes (\code{H^1}-consistency, gsma_notes Thm 3.1); a
nonzero \code{omega} on a cycle edge injects exactly the loop inconsistency
that the GSMA harmonic statistic must detect (gsma_notes Thm 4.3).
}
