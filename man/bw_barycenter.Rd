% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_barycenter.R
\name{bw_barycenter}
\alias{bw_barycenter}
\title{Bures-Wasserstein barycenter of Gaussian studies}
\usage{
bw_barycenter(studies, weights = NULL, tol = 1e-12, max_iter = 500L, S0 = NULL)
}
\arguments{
\item{studies}{List of \code{igmi_gaussian} objects.}

\item{weights}{Positive weights (default equal); normalised internally.}

\item{tol}{Convergence tolerance on \code{D(S)}.}

\item{max_iter}{Maximum iterations.}

\item{S0}{Starting covariance (default: linear mean of the
\code{Sigma_i}).}
}
\value{
Object of class \code{igmi_barycenter}: list with \code{theta}, \code{Sigma},
\code{weights}, \code{D} (final decrement), \code{iterations}, \code{converged},
\code{F_cov} (trace of objective values), \code{fixed_point_residual}.
}
\description{
Mean: \verb{theta_bar = sum_i w_i theta_i} (mean decoupling, igmi_notes
Lem 3.3). Covariance: the fixed point of
\verb{Sigma = sum_i w_i (Sigma^\{1/2\} Sigma_i Sigma^\{1/2\})^\{1/2\}}
(Thm 3.4) computed by the iteration \verb{S <- G(S) = Tbar S Tbar},
\verb{Tbar(S) = sum_i w_i T^\{S -> Sigma_i\}}, which converges
unconditionally from any positive-definite start (Thm 7.5 + Cor 7.8).
}
\details{
Stopping criterion: \verb{D(S) = ||(I - Tbar(S)) S^\{1/2\}||_F^2}, which is
the exact one-step decrement of the covariance functional (Lem 7.3)
and vanishes exactly at the barycenter (Thm 7.4). Two exact
monotonicity invariants are monitored every step and a warning is
thrown if either fails beyond roundoff: \code{F_cov(S_k)} nonincreasing
(Lem 7.3) and \verb{tr S_k} nondecreasing (Alvarez-Esteban et al. 2016,
eq. (21); close-reading note after Cor 7.8).
}
