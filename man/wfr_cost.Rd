% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_wfr.R
\name{wfr_cost}
\alias{wfr_cost}
\title{WFR ground cost matrix}
\usage{
wfr_cost(x, y, delta, cap = 1e+06)
}
\arguments{
\item{x, y}{Support points: numeric vectors (m = 1) or matrices with
one point per row.}

\item{delta}{WFR length scale.}

\item{cap}{Finite stand-in for \code{+Inf} beyond the cutoff.}
}
\value{
List: \code{M} (cost matrix), \code{capped} (logical matrix).
}
\description{
\verb{l_delta(x, y) = -log cos^2(min(||x - y|| / (2 delta), pi/2))}
(igmi_notes Rem 4.9(a)); the \code{+Inf} beyond the cutoff \verb{pi delta} is
encoded as the finite \code{cap} (Rem 4.9(b)(ii)) -- callers must check
that the returned plan puts no mass on capped pairs, which
\code{\link[=wfr_dist]{wfr_dist()}} does.
}
