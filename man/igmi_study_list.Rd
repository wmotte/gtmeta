% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_adapter.R
\name{igmi_study_list}
\alias{igmi_study_list}
\title{Group tidy estimates into per-study Gaussian inputs for IGMI}
\usage{
igmi_study_list(
  tidy,
  review_id,
  outcome_ids,
  rho = 0,
  duplicates = c("pool", "first", "error")
)
}
\arguments{
\item{tidy}{Tidy data frame from \code{\link[=cochrane_tidy]{cochrane_tidy()}}.}

\item{review_id}{Review to extract.}

\item{outcome_ids}{Character vector (length m >= 1) of \code{outcome_id}s.}

\item{rho}{Plug-in within-study correlation between outcomes.}

\item{duplicates}{Policy for subgroup rows duplicated within
\verb{(study, outcome_id)}: \code{"pool"}, \code{"first"}, or \code{"error"}.}
}
\value{
List of study objects \code{list(study =, theta =, Sigma =)}.
}
\description{
Collects, within one review, the outcomes listed in \code{outcome_ids} per
study into a mean vector and covariance matrix, giving the
\code{N(theta_i, Sigma_i)} study objects of the IGMI notes (igmi_notes
section 1). Within-study cross-outcome correlations are not recoverable
from the corpus, so \code{Sigma_i} is built from the reported variances with
a plug-in common correlation \code{rho} (default 0, i.e. diagonal);
sensitivity over \code{rho} is the intended use. Only studies observed on
\emph{all} requested outcomes are returned.
}
\details{
On the real corpus 14\% of rows are subgroup duplicates within
\verb{(study, outcome_id)} (RevMan subgroup rows). \code{duplicates} selects the
policy: \code{"pool"} (default) combines them by fixed-effect
inverse-variance pooling - exactly what RevMan does when totalling
subgroups of one study; \code{"first"} keeps the first row; \code{"error"}
refuses.
}
