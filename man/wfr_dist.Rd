% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_wfr.R
\name{wfr_dist}
\alias{wfr_dist}
\title{WFR distance between discrete measures (exact solver via POT)}
\usage{
wfr_dist(a, x, b, y, delta, max_iter = 20000L, tol = 1e-15)
}
\arguments{
\item{a, b}{Nonnegative masses.}

\item{x, y}{Support points (vectors or matrices, one point per row).}

\item{delta}{WFR length scale.}

\item{max_iter, tol}{Solver control.}
}
\value{
List: \code{wfr2} (squared distance), \code{wfr}, \code{plan}, \code{delta}.
}
\description{
Computes \verb{WFR_delta(sum a_i delta_\{x_i\}, sum b_j delta_\{y_j\})} by the
entropy-transport form (igmi_notes Rem 4.9(a)): minimize
\verb{KL(gamma_1 | a) + KL(gamma_2 | b) + <gamma, l_delta>} over
nonnegative plans, value times \verb{4 delta^2}. Uses POT's exact
majorization-minimization solver (\code{mm_unbalanced}, \code{div = 'kl'},
\code{reg = 0}); the objective is recomputed from the returned plan with
the unnormalized KL, so the result is a certified upper bound that
matches the optimum to solver tolerance. Errors if the plan puts
mass on beyond-cutoff pairs (Rem 4.9(b)(ii)).
}
