% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_inference.R
\name{igmi_bootstrap}
\alias{igmi_bootstrap}
\title{Nonparametric bootstrap over studies for the IGMI barycenter}
\usage{
igmi_bootstrap(
  studies,
  weights = NULL,
  B = 2000L,
  weight_fun = igmi_precision_weights,
  level = 0.95
)
}
\arguments{
\item{studies}{List of \code{igmi_gaussian} objects.}

\item{weights}{Positive weights (default equal).}

\item{B}{Number of bootstrap resamples.}

\item{weight_fun}{Function \code{studies -> weights} applied to each
resample; default \code{\link[=igmi_precision_weights]{igmi_precision_weights()}}. Pass \code{NULL} to keep
the (resampled) original weights.}

\item{level}{Confidence level for the percentile intervals.}
}
\value{
List: \code{theta_draws} (B x m), \code{scale_draws} (B, \verb{tr Sigma_bar}),
\code{theta_ci} (percentile, per component), \code{scale_ci}, \code{B}.
}
\description{
The Phase-3 default of igmi_notes Rem 5.15(a): resample the pairs
\verb{(theta_i, Sigma_i)} with replacement, recompute the
Bures-Wasserstein barycenter by the fixed-point iteration, and report
percentile intervals. Weights are recomputed on each resample when
\code{weight_fun} is supplied (default: precision weights), matching the
plug-in practice whose leading-order cost is quantified by
\code{\link[=igmi_weight_correction]{igmi_weight_correction()}}.
}
