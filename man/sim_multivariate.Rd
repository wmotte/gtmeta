% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_multivariate}
\alias{sim_multivariate}
\title{Simulate a multivariate (multi-outcome) meta-analysis}
\usage{
sim_multivariate(k, mu, Tau, rho_within = 0, sei_range = c(0.1, 0.5))
}
\arguments{
\item{k}{Number of studies.}

\item{mu}{True mean vector (length m).}

\item{Tau}{Between-study covariance (m x m, positive semidefinite).}

\item{rho_within}{Common within-study cross-outcome correlation.}

\item{sei_range}{Range for per-outcome within-study standard errors.}
}
\value{
List: \code{y} (k x m matrix of estimates), \code{Sigma} (list of k
within-study covariances), \code{theta} (k x m true effects), \code{mu}, \code{Tau}.
}
\description{
Truth: \code{theta_i ~ N_m(mu, Tau)},
\code{y_i | theta_i ~ N_m(theta_i, Sigma_i)} with
\verb{Sigma_i = D_i R(rho_within) D_i}, \code{D_i = diag(sei_i)} drawn per
outcome from \code{sei_range} and \code{R} an equicorrelation matrix. This is
the generating regime of the IGMI study objects \code{N(theta_i, Sigma_i)}
(igmi_notes section 1); the returned \code{Sigma} list feeds the
barycenter code directly.
}
