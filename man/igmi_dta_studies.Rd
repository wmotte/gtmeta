% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_dta.R
\name{igmi_dta_studies}
\alias{igmi_dta_studies}
\title{Diagnostic-accuracy study objects from 2x2 counts}
\usage{
igmi_dta_studies(tp, fn, fp, tn, study = NULL, cc = 0.5)
}
\arguments{
\item{tp, fn, fp, tn}{Equal-length numeric vectors of true positives, false
negatives, false positives, true negatives (diseased: \code{tp}, \code{fn};
non-diseased: \code{tn}, \code{fp}).}

\item{study}{Optional study labels (defaults to \code{seq_along(tp)}).}

\item{cc}{Continuity correction added to all four cells of any study
with a zero cell (default \code{0.5}); set \code{0} to disable.}
}
\value{
List of study objects \code{list(study =, theta =, Sigma =)}, the
same contract as \code{\link[=igmi_study_list]{igmi_study_list()}}; convert to \code{igmi_gaussian}
objects with \code{lapply(x, function(s) igmi_gaussian(s$theta, s$Sigma))}.
}
\description{
Turns per-study diagnostic 2x2 tables into IGMI study objects on the
logit scale (igmi_notes Definition 8.1). Each study becomes
\code{N(theta_i, Sigma_i)} with
\verb{theta_i = (logit Se_i, logit Sp_i)} and \strong{diagonal}
\code{Sigma_i = diag(1/TP + 1/FN, 1/TN + 1/FP)}; the off-diagonal is exactly
zero because sensitivity and specificity come from disjoint patient
groups (Lemma 8.2), so unlike the generic multivariate case there is no
plug-in within-study correlation here.
}
\details{
Cells are corrected before transformation only in studies that contain a
zero cell: \code{cc} (default 1/2) is added to all four cells of such a study
(Remark 8.3). Studies with a zero group margin after correction are
dropped with a warning.
}
\seealso{
\code{\link[=igmi_sroc]{igmi_sroc()}}, \code{\link[=igmi_study_list]{igmi_study_list()}}
}
