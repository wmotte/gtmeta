% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_wfr.R
\name{wfr_pool}
\alias{wfr_pool}
\title{WFR pooling: the one-atom WFR Frechet mean of the study estimates}
\usage{
wfr_pool(yi, sei = NULL, delta, weights = NULL, n_grid = 512L)
}
\arguments{
\item{yi}{Study effect estimates.}

\item{sei}{Study standard errors; used for the default precision
weights (may be omitted when \code{weights} is supplied).}

\item{delta}{WFR length scale (the robustness tuning constant:
rejection point \verb{pi delta} on the \code{yi} scale). \code{Inf} gives the
exact fixed-effect / W2 limit.}

\item{weights}{Optional Frechet weights (default \code{1 / sei^2});
normalized internally.
The \code{se} field is the classical M-estimation sandwich standard error
(Huber 1964; Godambe form, Hampel et al. 1986) of the stationarity
equation \verb{sum_i lambda_i sin((y_i - x)/(2 delta)) = 0} over the
active set (igmi_notes Rem 4.9, Phase-5.B update):
\verb{se^2 = (2 delta)^2 sum_A lambda_i^2 sin^2(u_i/(2 delta)) / (sum_A lambda_i cos(u_i/(2 delta)))^2}. As \code{delta -> Inf} it reduces
to the HC0 variance of the weighted mean
\verb{sqrt(sum lambda_i^2 (y_i - x_hat)^2)}, and for equal weights it is
invariant under adding studies beyond the rejection point. First
order only: the active set is treated as fixed (psi jumps at the
cutoff), valid when little data mass sits near the boundary.}

\item{n_grid}{Grid size for the global search.}
}
\value{
List: \code{estimate}, \code{se} (sandwich standard error),
\code{mass_ratio} (retained mass \code{b_hat},
1 = nothing killed), \code{value} (Frechet objective at the optimum),
\code{g_max}, \code{active} (logical: within the cutoff of the estimate),
\code{n_active}, \code{delta}, \code{weights} (normalized).
}
\description{
Robust pooled estimate from the WFR geometry: place each study as a
unit-mass atom \verb{delta_\{y_i\}}, and minimize the Frechet objective
\verb{sum_i lambda_i WFR_delta^2(delta_\{y_i\}, b delta_x)} over a single
candidate atom (location \code{x}, mass \code{b}). By the two-atom closed form
(igmi_notes Prop 4.8) the objective is
\verb{4 delta^2 sum_i lambda_i (1 + b - 2 sqrt(b) c_i(x))} with
\verb{c_i(x) = cos(min(|y_i - x| / (2 delta), pi/2))}; the first-order
condition in the mass gives \verb{sqrt(b_hat) = g(x) := sum_i lambda_i c_i(x)}, profiled value \verb{4 delta^2 (1 - g(x)^2)}, so the estimate is
\verb{argmax_x g(x)} (always inside \verb{[min yi, max yi]}: moving toward the
hull increases every \code{c_i}).
}
\details{
Two exact structural facts drive its use in Phase 4.B:
\itemize{
\item \verb{argmax g} is a redescending M-estimator of Andrews sine-wave type
(\code{psi(u)} proportional to \verb{sin(u / (2 delta))} for \verb{|u| <= pi delta}, zero beyond): a study farther than the transport cutoff
\verb{pi delta} from the estimate (Prop 4.8(2)) has exactly zero
influence -- the geometry kills its mass instead of transporting it.
\item As \code{delta -> Inf}, \code{WFR_delta} increases to \code{W_2} (igmi_notes
Thm 4.12 / Cor 4.13) and the objective becomes the weighted sum of
\code{(y_i - x)^2}, so with the default precision weights the estimate
converges to the fixed-effect estimate -- which is also the scalar
Bures-Wasserstein barycenter mean (Cor 3.6). \code{delta} therefore
interpolates between FE pooling and robust mode-seeking; the
breakdown behaviour is quantified in \code{sim/sim_contamination.R}.
}

Following igmi_notes Rem 4.15 (report far-field mass, never
renormalize), the retained-mass fraction \code{mass_ratio = g(x_hat)^2 = b_hat < 1} is returned as a contamination diagnostic, together with
the set of studies within the cutoff.

The profiled objective can be multimodal (as for any redescending
M-estimator); the maximizer is located on an \code{n_grid} grid over
\verb{[min yi, max yi]} and refined with \code{\link[stats:optimize]{stats::optimize()}} around the
best grid point, so \code{n_grid} must resolve the bump width \verb{2 pi delta}.
}
