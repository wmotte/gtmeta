% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igmi_dta.R
\name{igmi_sroc}
\alias{igmi_sroc}
\title{IGMI summary ROC (SROC) curve from a pooled DTA Gaussian}
\usage{
igmi_sroc(barycenter, T_hat, k = 2, n = 200L)
}
\arguments{
\item{barycenter}{Either a \code{\link[=bw_barycenter]{bw_barycenter()}} result (its \verb{$theta} is
used) or a length-2 numeric \verb{c(logit Se, logit Sp)}.}

\item{T_hat}{2x2 between-study covariance on the logit scale, e.g.
\code{igmi_mm_cov(studies, weights)$T_hat}.}

\item{k}{Half-width of the plotting range in SD units of logit Sp
(default 2).}

\item{n}{Number of curve points (default 200).}
}
\value{
Data frame with columns \verb{logit_sp, logit_se, spec, sens, fpr, tpr}; the summary operating point is attached as attribute
\code{"summary_point"} (a named numeric \code{c(sens, spec)}), and the slope as
attribute \code{"slope"}.
}
\description{
Reads the summary ROC curve off the pooled diagnostic Gaussian
(igmi_notes Definition 8.7): the conditional-mean line of
\code{N(theta_bar, T_hat)},
\verb{logit Se = mu_S + (t_SC / t_CC)(logit Sp - mu_C)},
mapped through \code{expit} into ROC coordinates. By Proposition 8.8 this is
the Reitsma bivariate-model SROC evaluated at \verb{(theta_bar, T_hat)}.
}
\details{
The mean order must be \verb{(logit Se, logit Sp)}, as produced by
\code{\link[=igmi_dta_studies]{igmi_dta_studies()}} and carried through \code{\link[=bw_barycenter]{bw_barycenter()}}.
}
\seealso{
\code{\link[=igmi_dta_studies]{igmi_dta_studies()}}, \code{\link[=igmi_mm_cov]{igmi_mm_cov()}}
}
