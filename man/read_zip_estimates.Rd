% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_adapter.R
\name{read_zip_estimates}
\alias{read_zip_estimates}
\title{Read zip-derived estimates}
\usage{
read_zip_estimates(path, level = 0.95)
}
\arguments{
\item{path}{Path to \code{zip_data.csv.gz}.}

\item{level}{Confidence level of the reported CIs (RevMan default 0.95).}
}
\value{
Tidy data frame (one row per study-by-outcome estimate).
}
\description{
Reads \code{zip_data.csv.gz} as written by \verb{08_proccess_estimates.R} of
cochrane2025rob. Effects are taken, in order of preference:
generic inverse-variance columns (\code{GIV.Mean}, \code{GIV.SE}; already on the
analysis scale), else reconstructed from the displayed \code{Mean} +
\code{CI.start}/\code{CI.end} with the scale inferred by \code{\link[=infer_ci_scale]{infer_ci_scale()}}
(Wald: \code{sei = (hi - lo) / (2 * qnorm(1 - (1 - level) / 2))} on the
inferred scale). Rows with neither are dropped with a message.
}
